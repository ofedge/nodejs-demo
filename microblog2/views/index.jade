extends layout

block content
  - if(!user)
    div.hero-unit
      h1 欢迎来到 Microblog
      p Microblog 是一个基于 Node.js 的微博系统.
      p
        a(class="btn btn-primary btn-large" href="/login") 登录
        a(class="btn btn-large" href="/reg") 立即注册
  - else
    include ./say
  include ./posts
